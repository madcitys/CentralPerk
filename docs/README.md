# Docs

This folder is reference-only.

It is not part of the active Docker runtime, frontend build, or backend startup flow.

## Structure

```text
docs/
  archive/
    legacy-frontend/
    legacy-services/
  loyalty-sprint5-microservice-dependencies.md
  loyalty-sprint5-microservice-dependencies.txt
```

## Notes

- `archive/` contains old reference code kept for lookup only.
- The active application code lives in `src/frontend` and `src/backend`.
- The folder is hidden in the shared VS Code workspace settings to keep the root cleaner.
